/** @type {import('prettier').Config} */
const config = {
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  arrowParens: 'avoid',
  proseWrap: 'always',
};

module.exports = config;
